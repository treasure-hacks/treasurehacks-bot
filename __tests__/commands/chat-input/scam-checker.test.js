/* const { ChannelType } = require('discord.js')
// Mocks must come before command imports
const discordMock = require('../../../.jest/mock-discord')
const detaMock = require('../../../.jest/mock-deta')
// Command Imports must come after mocks
const { setScannerStatus, setMaxUserAge, updateIgnoredRoles, replyWithStatus } = require('../../../src/commands/chat-input/scam-checker') */

describe.skip('Scam Checker Command', () => {
  // skip this test for now since it's still not operating in production
  it('works correctly', () => { })
})
